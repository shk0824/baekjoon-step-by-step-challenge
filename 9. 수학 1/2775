T=int(input())
for t in range(T):
    k=int(input())
    n=int(input())
    h=[[0]*n for _ in range(k+1)]
    for a in range(k+1):
        for b in range(n):
            if a==0:
                h[a][b]=b+1
            else:
                h[a][b]=sum(h[a-1][:b+1])
    print(h[k][n-1])
