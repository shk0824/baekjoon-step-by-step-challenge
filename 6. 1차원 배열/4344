t = int(input())
for i in range(t):
    a = list(map(int, input().split()))
    b = sum(a[1:])/a[0]
    c = 0
    for i in a[1:]:
        if i > b:
            c += 1
    d = c/a[0]*100
    print("%0.3f" % d + "%")
